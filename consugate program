# Check if a list has consecutive numbers
def are_consecutive(nums):
    return sorted(nums) == list(range(min(nums), max(nums) + 1))
nums = [4, 5, 6, 7]
if are_consecutive(nums):
    print("Numbers are consecutive")
else:
    print("Numbers are not consecutive")
